[build]
  publish = "."

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Clean path for the try-on proxy (preserves method + query string).
[[redirects]]
  from = "/api/tryon"
  to = "/.netlify/functions/tryon"
  status = 200
